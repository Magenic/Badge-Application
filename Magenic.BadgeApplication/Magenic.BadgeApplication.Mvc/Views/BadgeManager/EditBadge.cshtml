@using Magenic.BadgeApplication.Resources
@using Magenic.BadgeApplication.Models
@model BadgeEditViewModel

<div class="container">
    <div class="row">
        @Html.Partial("_AdminNavbar")
    </div>
    <div class="row">
        <div class="col-md-12 grayBox">
            <div class="row">
                <div class="col-md-12">
                    <h2>@ApplicationResources.EditBadgeHeader</h2>
                </div>
            </div>
            @using (Html.BeginForm("EditBadgePost", "BadgeManager", new { id = Model.Badge.Id }, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.EditorFor(bevw => bevw.Badge)
            }
        </div>
    </div>
    <div class="row">
        &nbsp;
    </div>
</div>

@section scripts {
    @Scripts.Render("~/bundles/badgeManager")
}